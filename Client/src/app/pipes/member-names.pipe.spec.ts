import { MemberNamesPipe } from './member-names.pipe';

describe('MemberNamesPipe', () => {
  it('create an instance', () => {
    const pipe = new MemberNamesPipe();
    expect(pipe).toBeTruthy();
  });
});
